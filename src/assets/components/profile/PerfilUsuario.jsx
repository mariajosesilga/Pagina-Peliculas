import { Box, styled} from "@mui/material";

const PerfilUsuario = styled(Box)(({ theme }) => ({
  display: "flex",
  flexWrap: "wrap",
  flexDirection: "column",
  alignItems: "center",
}));

export default PerfilUsuario;